import Constants from 'expo-constants';
import { Platform } from 'react-native';

// TEMPORARY FIX: Hardcode localhost for web
const BACKEND_BASE = `https://test.ifda.in`;

export { BACKEND_BASE };
export default { BACKEND_BASE };
